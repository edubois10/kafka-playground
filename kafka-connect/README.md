### Connect API
```
# Get the connectors list.
# Inside the connect pod
curl -X GET localhost:8083/connectors
```

```
# Gets the connector detail
# Inside the connect pod
curl -X GET localhost:8083/connectors/<connector>
```